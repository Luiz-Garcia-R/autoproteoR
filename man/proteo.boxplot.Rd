% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proteoBoxplot.R
\name{proteo.boxplot}
\alias{proteo.boxplot}
\title{Statistical test for protein abundance between two groups}
\usage{
proteo.boxplot(
  normalized_data,
  metadata = NULL,
  protein_id,
  group_col = "Group",
  return_type = c("all", "htest")
)
}
\arguments{
\item{normalized_data}{A normalized abundance data frame (containing \code{ProteinID})
or an object of class \code{proteonorm} (output of \code{proteo.normalize()}).}

\item{metadata}{A data frame with sample metadata. Must contain a \code{Sample} column
and the grouping column specified in \code{group_col}.}

\item{protein_id}{Character. The ID of the protein to test (must be present in \code{normalized_data}).}

\item{group_col}{Character. Column name in \code{metadata} defining the groups
(default = \code{"Group"}). Only two groups are currently supported.}

\item{return_type}{Either \code{"htest"} (returns only the test result) or \code{"all"}
(returns test, data, and plot). Default = \code{"htest"}.}
}
\value{
Invisibly returns:
\itemize{
\item if \code{return_type = "htest"}: an object of class \code{htest} (t-test or Wilcoxon test).
\item if \code{return_type = "all"}: a list with:
\describe{
\item{test}{The statistical test result (htest object).}
\item{data}{Long-format data frame used for the test and plotting.}
\item{plot}{A ggplot2 object with boxplot + jitter.}
}
}
}
\description{
Performs a limma t moderated test for the normalized abundance of a specific protein across two groups.
Returns a significance summary and a boxplot with jittered points.
}
\examples{
\dontrun{
# Small example: normalized data
normalized_data <- data.frame(
  ProteinID = paste0("P", 1:5),
  Control1 = c(100, 200, 150, 300, 250),
  Control2 = c(110, 210, 160, 310, 260),
  Treatment1 = c(300, 100, 200, 150, 250),
  Treatment2 = c(310, 90, 210, 140, 260)
)

metadata <- data.frame(
  Sample = c("Control1", "Control2", "Treatment1", "Treatment2"),
  Group = c("Control", "Control", "Treatment", "Treatment")
)

# Perform limma t-test for protein "P1"
result <- proteo.boxplot(normalized_data, protein_id = "P1")
}

}
\references{
Student, W. (1908). The probable error of a mean. \emph{Biometrika}, 6(1), 1–25. \url{doi:10.1093/biomet/6.1.1}
Mann, H. B., & Whitney, D. R. (1947). On a test of whether one of two random variables is stochastically larger than the other. \emph{Ann. Math. Stat.}, 18, 50–60. \url{doi:10.1214/aoms/1177730491}
}
