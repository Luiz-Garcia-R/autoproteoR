% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoproteoR-package.R
\docType{package}
\name{autoproteoR}
\alias{autoproteoR-package}
\alias{autoproteoR}
\title{autoproteoR: Quick Reference and Main Functions for Proteomics Analysis}
\description{
autoproteoR provides a complete suite of functions for proteomics data analysis,
from raw data import to exploratory and statistical analysis. This help topic
serves as a quick reference and guide for users.
}
\details{
Main Functions Overview\tabular{ll}{
   Function \tab Description \cr
   \code{proteo.import()} \tab Import and validate raw proteomics data \cr
   \code{proteo.normalize()} \tab Normalize and filter data \cr
   \code{proteo.qc()} \tab QC plots (boxplot, PCA) \cr
   \code{proteo.corr()} \tab Compute sample correlations \cr
   \code{proteo.dimred()} \tab PCA and UMAP dimensionality reduction \cr
   \code{proteo.enrich()} \tab GO enrichment analysis \cr
   \code{proteo.heatmap()} \tab For top variable proteins \cr
   \code{proteo.ttest()} \tab T-test or Mann-Whitney test \cr
   \code{proteo.venn()} \tab Venn diagrams \cr
   \code{proteo.volcano()} \tab For differentially expressed proteins \cr
   \code{proteo.roc()} \tab ROC curve for classification analysis \cr
}


Contact and Contributions
For suggestions, bug reports, or contributions, see the
\href{https://github.com/Luiz-Garcia-R/autoproteoR}{GitHub repository}.

Example workflow for proteo.import and proteo.normalize

A small synthetic proteomics dataset with 50 proteins and 5 samples per group.
This dataset is for demonstration purposes only.
}
\examples{

proteins <- paste0("P", sprintf("\%03d", 1:50))

raw_data <- data.frame(
  ProteinID = proteins,
  Control_1   = rnorm(50, mean = 10000, sd = 2000),
  Control_2   = rnorm(50, mean = 10000, sd = 2000),
  Control_3   = rnorm(50, mean = 10000, sd = 2000),
  Control_4   = rnorm(50, mean = 10000, sd = 2000),
  Control_5   = rnorm(50, mean = 10000, sd = 2000),
  Treatment_1 = rnorm(50, mean = 20000, sd = 2500),
  Treatment_2 = rnorm(50, mean = 20000, sd = 2500),
  Treatment_3 = rnorm(50, mean = 20000, sd = 2500),
  Treatment_4 = rnorm(50, mean = 30000, sd = 2500),
  Treatment_5 = rnorm(50, mean = 30000, sd = 2500)
)

metadata <- data.frame(
  Sample = colnames(raw_data)[-1],
  Group  = rep(c("Control", "Treatment"), each = 5)
)


obj <- proteo.import(raw_data, metadata)
normalized <- proteo.normalize(raw_data, metadata)

proteo.qc(normalized, metadata)

corr_mat <- proteo.corr(normalized, metadata)
dimred_res <- proteo.dimred(normalized, metadata)

proteo.volcano(normalized, metadata,
               group_col = "Group",
               padj_threshold = 0.05,
               log2fc_threshold = 1,
               results = TRUE)

}
\author{
\strong{Maintainer}: Luiz Garcia \email{luiz.cardoso@ufpr.br} (\href{https://orcid.org/0000-0002-9616-0927}{ORCID}) (UFPR)

}
